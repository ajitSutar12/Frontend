import { FoundPaymentTransactionDirective } from './found-payment-transaction.directive';

describe('FoundPaymentTransactionDirective', () => {
  it('should create an instance', () => {
    const directive = new FoundPaymentTransactionDirective();
    expect(directive).toBeTruthy();
  });
});
